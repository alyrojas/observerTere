import { EmpleadofilterPipe } from './empleadofilter.pipe';

describe('EmpleadofilterPipe', () => {
  it('create an instance', () => {
    const pipe = new EmpleadofilterPipe();
    expect(pipe).toBeTruthy();
  });
});
